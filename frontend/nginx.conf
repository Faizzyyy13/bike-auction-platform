server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # This routes all frontend requests to your Vite app
    location / {
        try_files $uri $uri/ /index.html;
    }
}
